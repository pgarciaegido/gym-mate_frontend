/* eslint-disable */
import React from 'react';
import App from './app';
import {shallow} from 'enzyme';

describe('Easy one', () => {
    
    it('Should sum 2 + 2', () => {
        expect(2 + 2).toBe(4);
    })
})
